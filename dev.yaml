---
- hosts: all
  become: true
  become_user: root
  become_method: sudo
  tasks:
    - name: Detect architecture
      set_fact:
        arch: "{{ 'arm64' if ansible_facts.architecture == 'aarch64' else 'amd64' }}"
    - name: Upgrade vulkan-sdk
      import_tasks: "tasks/vulkan-sdk.yaml"
      vars:
        uninstall_previous_version: true
        vulkansdk_path: /opt/local/vulkan-sdk
      when:
        lookup('ansible.builtin.env', 'NOVULKAN', default='0') != '1' and
        arch == 'amd64'
    - name: Install golang
      import_tasks: "tasks/golang.yaml"
      vars:
        go_version: 1.26.2
        go_checksum_amd64: sha256:990e6b4bbba816dc3ee129eaeaf4b42f17c2800b88a2166c265ac1a200262282
        go_checksum_arm64: sha256:c958a1fe1b361391db163a485e21f5f228142d6f8b584f6bef89b26f66dc5b23
        go_path: /opt/local
      when:
        lookup('ansible.builtin.env', 'NOGO', default='0') != '1'
